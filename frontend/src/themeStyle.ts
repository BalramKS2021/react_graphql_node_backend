import { createTheme } from "@mui/material/styles";

const ThemeStyle = () =>
  createTheme({
    palette: {
      primary: {
        main: "#02afaa",
      },
    },
  });
export default ThemeStyle;
